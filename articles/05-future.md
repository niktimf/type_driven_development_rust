# Type-driven development в Rust. Часть 5/5: заглядываем в будущее — substructural types, эффекты, variadic generics, view types

_Черновик в работе. Пока только замысел части._

Планируется:
- Substructural (linear / affine) types — владение и обязательства на уровне типа: заявку или зарезервированную маржу нельзя молча уронить, её обязаны исполнить либо явно отменить/освободить. Affine у Rust уже есть (move + drop); linear («ровно один раз») — обсуждаемое направление.
- Эффекты (effect systems) — функции с гарантиями (no-panic, детерминизм): одна торговая логика под разными интерпретаторами — бэктест и реальные торги. Официально нащупывает [keyword generics initiative](https://rust-lang.github.io/keyword-generics-initiative/).
- Variadic / tuple generics — обобщение по переменному числу типов (трейты и функции над кортежами любой длины): нативное будущее того, что в Части 3 эмулируется через HList. Давно обсуждается, с новым trait-солвером снова в повестке lang-team.
- View types (частичные заимствования) — в типе записано, какие поля доступны (`{a, b} Self`): можно независимо одолжить непересекающиеся части структуры. Делает явным то, что борроу-чекер сейчас выводит молча. Proposal-стадия (Niko Matsakis, 2021).

---

<!-- Финал ВСЕЙ серии. Изначально жил в конце Части 1, временно стоял в конце Части 4; переехал сюда как концовка последней части. Черновик, не отредактирован под голос серии. -->

Хорошая модель домена — это не та, где можно выразить всё подряд.
Скорее наоборот: та, в которой трудно сделать неправильную вещь.
Если модель строится вокруг примитивов и независимых полей, типы обычно позволяют больше, чем реально существует в предметной области.
Тогда ограничения начинают жить отдельно — в if-ах, документации, комментариях и негласных договорённостях вроде «этот метод не вызывай после cancel».
Получается разрыв: типы разрешают больше состояний, чем существует в модели.

Type-driven подход пытается этот разрыв закрыть.
Типы начинают описывать не только форму данных, но и правила игры: какие состояния возможны, какие комбинации допустимы, какие переходы разрешены.
У этого есть цена. Типов становится больше, модель — подробнее, а код многословнее.
Но сложность не исчезает — она просто переезжает: из runtime-проверок в систему типов.
Вместо «не забудь проверить этот кейс» появляется ошибка компиляции.
Вместо документации в духе «сюда передавай только валидное состояние» — интерфейс, который не даёт передать невалидное.