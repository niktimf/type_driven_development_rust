[package]
name = "tdd-01-foundations"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
argon2 = { version = "0.5", features = ["std"] }
rust_decimal = { version = "1", features = ["macros"] }
